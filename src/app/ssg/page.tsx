const { family, familySync, GLIBC, MUSL } = require('detect-libc');

const Dir = async() => {
  return (
    <div>
      <pre className="whitespace-pre-line">
        <div>

        </div>
      </pre>
    </div>
  );
}

export default Dir;
